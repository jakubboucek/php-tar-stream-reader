{
    "require": {
        "php": ">=7.3",
        "ext-zlib": "*"
    },
    "autoload": {
        "psr-4": {
            "Tar\\": "libs/Tar/"
        }
    },
    "scripts": {
        "phpstan": "docker run --rm -v `pwd`:/www phpstan/phpstan analyse -c /www/phpstan.neon --level 6"
    }
}
